import React from 'react';
import ReactFlow from 'react-flow-renderer';

const BasicGraph = ({ elements, graphStyles }) => <ReactFlow elements={elements} style={graphStyles} />;
export default BasicGraph;